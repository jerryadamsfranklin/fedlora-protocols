_inherit: base_config_4layers.yaml

experiment:
  name: "exp_reverse_adaptive"
  description: "Reverse adaptive: FLoRA then FFA-LoRA when loss improvement slows"

federated:
  aggregation_method: "reverse_adaptive"

reverse_adaptive:
  switch_threshold: 0.01
  warmup_rounds: 5
  transition_rounds: 2
  stability_threshold: 1.1
