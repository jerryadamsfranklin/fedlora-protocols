_inherit: base_config_4layers.yaml

experiment:
  name: "exp_reverse_adaptive_iid"
  description: "Adaptive FLoRA -> FFA-LoRA with loss-plateau switching, IID Alpaca-3k"

federated:
  aggregation_method: "reverse_adaptive"

reverse_adaptive:
  switch_threshold: 0.01
  warmup_rounds: 5
  transition_rounds: 0
  stability_threshold: 1.1
