# Legacy scripts

Scripts in this directory predate the current paper and reference experimental configurations or figure layouts that are no longer canonical. They are kept for historical record.

For paper-aligned analysis and figures, use:

- `scripts/build_results_table.py` (master CSV)
- `scripts/statistical_analysis.py` (paired tests)
- `scripts/generate_paper_figures.py` (six paper-ready PDFs)
